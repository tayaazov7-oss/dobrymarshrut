document.addEventListener('DOMContentLoaded', function () {    var loginBtn = document.getElementById('loginBtn');    var addBtn   = document.getElementById('addInitiativeBtn');    if (loginBtn) {        loginBtn.addEventListener('click', function (e) {            e.preventDefault();            if (window.Modal && typeof Modal.openAuthModal === 'function') {                Modal.openAuthModal('login');            }        });    }    if (addBtn) {        addBtn.addEventListener('click', function (e) {            e.preventDefault();            if (window.Modal && typeof Modal.openAuthModal === 'function') {                Modal.openAuthModal('register-nko');            }        });    }});