🗺️ Карта добрых дел

Интерактивная карта НКО и социальных инициатив в городах присутствия Росатома

Карта добрых дел — это интерактивный веб-сервис, который объединяет данные о НКО, социальных проектах и волонтёрских инициативах малых городов.
Проект помогает жителям, волонтёрам и бизнесу быстро находить организации, которым нужна помощь, а НКО — рассказывать о себе и привлекать добровольцев.

⸻

🚀 Возможности платформы

👥 Для пользователей
	•	просмотр НКО в своём городе на интерактивной карте;
	•	быстрый поиск по названию, категориям и адресу;
	•	просмотр ближайших мероприятий;
	•	возможность зарегистрироваться и предложить новую инициативу.

🏢 Для НКО
	•	подача собственной организации на модерацию;
	•	публикация мероприятий и описаний проектов;
	•	обновление контактов, логотипа и информации.

🗺️ Карта
	•	Яндекс.Карты + отдельные метки для НКО;
	•	фокусирование на метку при клике на карточку;
	•	автоматическая фильтрация меток в зависимости от выбранного города;
	•	синхронизация списка слева и карты справа.

⸻

🛠 Технологический стек

Фронтенд
	•	HTML5, CSS3, JavaScript (ES6)
	•	Адаптивная вёрстка
	•	Компонентная архитектура:
	•	app.js — загрузка НКО и событий, фильтры, поиск
	•	map.js — инициализация Яндекс.Карты, метки
	•	modal.js — модальные окна (логин, регистрация, создание НКО)
	•	auth.js — логика авторизации и рендер шапки
	•	api.js — работа с API

Карта
	•	Yandex.Maps JavaScript API 2.1

Бэкенд
	•	Python 3.11 + Flask
	•	REST-API:
	•	/api/nko — список НКО
	•	/api/events — мероприятия
	•	/api/auth/* — регистрация, логин, получение текущего пользователя
	•	/api/nko/create — создание новой НКО

База данных
	•	SQLite (прототип)
	•	Возможность заменить на PostgreSQL в продакшне

Импорт из Excel
	•	parser.py
	•	Использует pandas / openpyxl
	•	Загружает НКО и мероприятия из Excel → в базу данных

⸻

⚙️ Структура проекта

project/
│
├── backend/
│   ├── server.py           # Flask-бэкенд
│   ├── database.db         # SQLite БД
│   ├── parser.py           # Импорт Excel → БД
│   └── ...  
│
├── frontend/
│   ├── index.html
│   ├── css/
│   │   ├── styles.css
│   │   └── map.css
│   ├── js/
│   │   ├── app.js
│   │   ├── auth.js
│   │   ├── modal.js
│   │   ├── api.js
│   │   └── map.js
│   └── img/
│       ├── icons/
│       ├── events/
│       └── ...
│
└── README.md


⸻

💡 Архитектура решения
	•	Приложение разделено на фронтенд и бэкенд части.
	•	Фронтенд — обычный статический сайт, обслуживаемый Flask.
	•	Бэкенд — REST-API + валидация данных.
	•	Яндекс.Карты интегрируются напрямую через JS API.
	•	Отдельный Excel-парсер обновляет БД перед запуском проекта.
	•	Система ролей:
	•	гость (просмотр)
	•	пользователь (регистрация)
	•	НКО-аккаунт (подача инициатив)
	•	администратор (модерация НКО → публикация)

⸻

🧩 Установка и запуск

1. Установка зависимостей

cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

2. Импорт данных из Excel

Поместите файл nko.xlsx и events.xlsx в backend/ и выполните:

python parser.py

3. Запуск сервера

python server.py

Сайт будет доступен на:

http://127.0.0.1:5000


⸻

📦 Особенности для конкурса
	•	Проект полностью воспроизводим локально.
	•	Имеет чёткое разделение на логические слои.
	•	Стек подобран так, чтобы любой участник мог поддерживать решение без сложного деплоя.
	•	Можно разворачивать на продакшн-сервере через gunicorn + nginx.
	•	Следует всем требованиям хакатона:
	•	есть карта;
	•	есть база данных НКО и мероприятий;
	•	есть форма подачи инициативы;
	•	есть регистрация / вход;
	•	есть связь со всеми страницами (карта, мероприятия, about, контакты);
	•	интеграция с Excel выполнена.

